# Cloudflare Worker URL
# For local development (default if not set):
# VITE_WORKER_URL=http://localhost:8787

# For production deployment:
# VITE_WORKER_URL=https://your-worker-name.your-account.workers.dev

# API Key for local development (required if set in Worker)
# VITE_API_KEY=your-dev-api-key-here
